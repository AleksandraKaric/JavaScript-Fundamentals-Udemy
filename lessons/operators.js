// Relational and comparison operators

// > more than
// < less than
// >= more than equal
// <= less than equal


var result = 6 >= 5  
console.log(result) // reultati su true ili false

// Equality operators

var x = 1

console.log(x == '1') // lose comparison - proverava samo vrednost, ne i tip varijable
console.log(x === 1) // strict comparison - proverava i vrednost i tip varijable